import React, { Component } from "react";
class FavoriteChannels extends Component {
  constructor(props) {
    super(props);
    this.state = {};
  }
  render() {
    return <h2>Favorite channels</h2>;
  }
}

export default FavoriteChannels;
